import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.regisapp2',
  appName: 'regisapp2',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
